-- ============================================
-- EXECUTOR MENU - Phiên bản Nền Xám Trắng Trong Suốt Sắn Kẻ (Caro)
-- (Banana Cat Hub Style + Caro Pattern + Chỉnh kích thước 4 góc)
-- StarterGui -> LocalScript
-- ============================================

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ==================== CONSTANTS ====================
local C = {
    WHITE = Color3.fromRGB(255, 255, 255),
    DARK = Color3.fromRGB(40, 40, 45),
    GRAY = Color3.fromRGB(110, 115, 125),
    GREEN = Color3.fromRGB(0, 170, 90),
    BLUE = Color3.fromRGB(0, 130, 220),
    RED = Color3.fromRGB(220, 60, 60),
    YELLOW = Color3.fromRGB(255, 200, 0),
    PURPLE = Color3.fromRGB(130, 60, 200),
    ORANGE = Color3.fromRGB(220, 130, 50),
    BG = Color3.fromRGB(240, 242, 248),
}

local DEFAULT_LIGHTING = {
    ClockTime = 14, Brightness = 1.2,
    OutdoorAmbient = Color3.fromRGB(180, 180, 180),
    Ambient = Color3.fromRGB(128, 128, 128),
    FogEnd = 100000, FogStart = 0, FogColor = Color3.new(1, 1, 1),
    GlobalShadows = true, EnvironmentDiffuseScale = 1,
    EnvironmentSpecularScale = 1, ShadowSoftness = 0.5,
}

local PRESETS = {
    Ultra = {level=21, shadow=0.2, diffuse=1, specular=1, shadows=true, name="✨ Siêu cao"},
    High = {level=17, shadow=0.5, diffuse=1, specular=1, shadows=true, name="🔵 Cao"},
    Medium = {level=10, shadow=1, diffuse=0.8, specular=0.6, shadows=true, name="🟡 Trung"},
    Low = {level=5, shadow=1, diffuse=0.5, specular=0.3, shadows=false, name="🟢 Thấp"},
    UltraLow = {level=1, shadow=1, diffuse=0, specular=0, shadows=false, name="⚡ Siêu thấp"},
}

-- ==================== UI HELPER ====================
local function New(cls, props, parent)
    local obj = Instance.new(cls)
    for k, v in pairs(props or {}) do
        obj[k] = v
    end
    if parent then
        obj.Parent = parent
    end
    return obj
end

local function Corner(p, r)
    return New("UICorner", {CornerRadius = r or UDim.new(0, 8)}, p)
end

local function Stroke(p, c, t)
    return New("UIStroke", {Color = c or Color3.fromRGB(170, 175, 190), Thickness = t or 1.2}, p)
end

local function Tween(o, p, d, e)
    TweenService:Create(o, TweenInfo.new(d or 1.5, e or Enum.EasingStyle.Quad), p):Play()
end

local function TweenL(p)
    Tween(Lighting, p)
end

-- ==================== SCREEN GUI ====================
local gui = New("ScreenGui", {
    Name="ExMenu",
    IgnoreGuiInset=true,
    ResetOnSpawn=false,
    ZIndexBehavior=Enum.ZIndexBehavior.Sibling,
}, playerGui)

-- Toggle Button
local togBtn = New("TextButton", {
    Size=UDim2.new(0,44,0,44),
    Position=UDim2.new(1,-56,1,-56),
    Text="🍌",
    BackgroundColor3=C.WHITE,
    BackgroundTransparency=0.2,
    TextColor3=C.DARK,
    Font=Enum.Font.GothamBold,
    TextSize=22,
    BorderSizePixel=0,
    ZIndex=10,
}, gui)
Corner(togBtn, UDim.new(1,0))
Stroke(togBtn, C.BLUE, 1.5)

-- Main Frame (Nền Xám Trắng Trong Suốt: 540x340)
local main = New("Frame", {
    Size=UDim2.new(0,540,0,340),
    Position=UDim2.new(0.5,-270,0.5,-170),
    BackgroundColor3=Color3.fromRGB(235, 238, 245),
    BackgroundTransparency=0.25, -- Trong suốt nhẹ
    BorderSizePixel=0,
    Visible=false,
    ClipsDescendants=false,
    ZIndex=2,
}, gui)
Corner(main, UDim.new(0,10))
Stroke(main, Color3.fromRGB(180,185,200), 2)

-- 🏁 TẠO HOA VĂN SẮN KẺ / CARO (CHECKERED PATTERN OVERLAY)
local bgPattern = New("ImageLabel", {
    Name = "CheckeredBG",
    Size = UDim2.new(1, 0, 1, 0),
    Position = UDim2.new(0, 0, 0, 0),
    BackgroundTransparency = 1,
    Image = "rbxassetid://9822602710", -- Tileable checkerboard asset
    ScaleType = Enum.ScaleType.Tile,
    TileSize = UDim2.new(0, 20, 0, 20),
    ImageTransparency = 0.82, -- Hoa văn caro chìm sang trọng
    ImageColor3 = Color3.fromRGB(150, 160, 185),
    ZIndex = 2,
}, main)
Corner(bgPattern, UDim.new(0, 10))

-- Title Bar
local titleBar = New("Frame", {
    Size=UDim2.new(1,0,0,30),
    BackgroundColor3=Color3.fromRGB(225,230,240),
    BackgroundTransparency=0.2,
    BorderSizePixel=0,
    ZIndex=3,
}, main)
Corner(titleBar, UDim.new(0,10))

New("TextLabel", {
    Size=UDim2.new(1,-40,1,0),
    Position=UDim2.new(0,12,0,0),
    Text="🍌 Banana Cat Executor Hub (Caro Style)",
    BackgroundTransparency=1,
    TextColor3=C.DARK,
    Font=Enum.Font.GothamBold,
    TextSize=13,
    TextXAlignment=Enum.TextXAlignment.Left,
    ZIndex=4,
}, titleBar)

local closeBtn = New("TextButton", {
    Size=UDim2.new(0,30,0,30),
    Position=UDim2.new(1,-32,0,0),
    Text="✕",
    BackgroundTransparency=1,
    TextColor3=Color3.fromRGB(120,120,130),
    Font=Enum.Font.GothamBold,
    TextSize=15,
    BorderSizePixel=0,
    ZIndex=4,
}, titleBar)

-- ===== DRAG =====
local dragging, dragStart, startPos
titleBar.InputBegan:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
        dragging=true
        dragStart=i.Position
        startPos=main.Position
    end
end)
UserInputService.InputChanged:Connect(function(i)
    if dragging and (i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then
        local d=i.Position-dragStart
        main.Position=UDim2.new(startPos.X.Scale, startPos.X.Offset+d.X, startPos.Y.Scale, startPos.Y.Offset+d.Y)
    end
end)
UserInputService.InputEnded:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
        dragging=false
    end
end)

-- ===== RESIZE TẠI 4 GÓC (4-CORNER RESIZING) =====
local minW, minH = 420, 260

local function SetupResizeHandle(btn, cornerType)
    local resizing, sizeStart, posStart, inputStart
    btn.InputBegan:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
            resizing=true
            sizeStart=main.Size
            posStart=main.Position
            inputStart=i.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if resizing and (i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then
            local d = i.Position - inputStart
            local w, h = sizeStart.X.Offset, sizeStart.Y.Offset
            local posX, posY = posStart.X.Offset, posStart.Y.Offset

            local newW, newH = w, h
            local newX, newY = posX, posY

            if cornerType == "BR" then
                newW = math.max(minW, w + d.X)
                newH = math.max(minH, h + d.Y)
            elseif cornerType == "BL" then
                newW = math.max(minW, w - d.X)
                newH = math.max(minH, h + d.Y)
                newX = posX + (w - newW)
            elseif cornerType == "TR" then
                newW = math.max(minW, w + d.X)
                newH = math.max(minH, h - d.Y)
                newY = posY + (h - newH)
            elseif cornerType == "TL" then
                newW = math.max(minW, w - d.X)
                newH = math.max(minH, h - d.Y)
                newX = posX + (w - newW)
                newY = posY + (h - newH)
            end

            main.Size = UDim2.new(sizeStart.X.Scale, newW, sizeStart.Y.Scale, newH)
            main.Position = UDim2.new(posStart.X.Scale, newX, posStart.Y.Scale, newY)
        end
    end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
            resizing=false
        end
    end)
end

-- Nút kéo 4 góc màu Xanh Dương
local function CreateHandle(icon, pos)
    local btn = New("TextButton", {
        Size=UDim2.new(0,20,0,20),
        Position=pos,
        Text=icon,
        BackgroundColor3=C.BLUE,
        BackgroundTransparency=0.1,
        TextColor3=C.WHITE,
        Font=Enum.Font.GothamBold,
        TextSize=11,
        BorderSizePixel=0,
        ZIndex=100,
    }, main)
    Corner(btn, UDim.new(0,5))
    Stroke(btn, Color3.fromRGB(255,255,255), 1.2)
    return btn
end

local handleTL = CreateHandle("↖", UDim2.new(0, 2, 0, 2))
local handleTR = CreateHandle("↗", UDim2.new(1, -22, 0, 2))
local handleBL = CreateHandle("↙", UDim2.new(0, 2, 1, -22))
local handleBR = CreateHandle("↘", UDim2.new(1, -22, 1, -22))

SetupResizeHandle(handleTL, "TL")
SetupResizeHandle(handleTR, "TR")
SetupResizeHandle(handleBL, "BL")
SetupResizeHandle(handleBR, "BR")

-- ==================== TAB SYSTEM ====================
local tabs = {}
local tabContent = {}

local tabBar = New("ScrollingFrame", {
    Size=UDim2.new(0,95,1,-30),
    Position=UDim2.new(1,-95,0,30),
    BackgroundColor3=Color3.fromRGB(225,230,240),
    BackgroundTransparency=0.3,
    BorderSizePixel=0,
    ZIndex=3,
    ScrollBarThickness=3,
    CanvasSize=UDim2.new(0,0,0,0),
}, main)

New("UIListLayout", {
    FillDirection=Enum.FillDirection.Vertical,
    SortOrder=Enum.SortOrder.LayoutOrder,
    Padding=UDim.new(0,4),
}, tabBar)

New("UIPadding", {PaddingTop=UDim.new(0,6), PaddingLeft=UDim.new(0,4)}, tabBar)

local contentArea = New("Frame", {
    Size=UDim2.new(1,-95,1,-30),
    Position=UDim2.new(0,0,0,30),
    BackgroundTransparency=1,
    BorderSizePixel=0,
    ZIndex=3,
}, main)

local activeTab = nil

local function AddTab(name, icon, order)
    local btn = New("TextButton", {
        Size=UDim2.new(1,-8,0,30),
        Text=icon.." "..name,
        BackgroundColor3=C.BG,
        BackgroundTransparency=0.3,
        TextColor3=C.DARK,
        Font=Enum.Font.GothamBold,
        TextSize=9,
        BorderSizePixel=0,
        LayoutOrder=order,
        TextXAlignment=Enum.TextXAlignment.Left,
        ZIndex=4,
    }, tabBar)
    Corner(btn, UDim.new(0,6))

    local sf = New("ScrollingFrame", {
        Size=UDim2.new(1,0,1,0),
        BackgroundTransparency=1,
        BorderSizePixel=0,
        ScrollBarThickness=5,
        ScrollBarImageColor3=Color3.fromRGB(120,120,140),
        ClipsDescendants=true,
        CanvasSize=UDim2.new(0,0,0,0),
        Visible=false,
        Active=true,
        Selectable=false,
        ScrollingDirection=Enum.ScrollingDirection.Y,
        ZIndex=4,
    }, contentArea)

    btn.MouseButton1Click:Connect(function()
        if activeTab==sf then return end
        for _, t in ipairs(tabContent) do t.Visible=false end
        for _, b in ipairs(tabs) do
            b.BackgroundColor3=C.BG
            b.BackgroundTransparency=0.3
        end
        sf.Visible=true
        btn.BackgroundColor3=C.BLUE
        btn.BackgroundTransparency=0.2
        activeTab=sf
    end)

    table.insert(tabs, btn)
    table.insert(tabContent, sf)
    tabBar.CanvasSize = UDim2.new(0, 0, 0, #tabs * 34 + 10)
    return sf
end

-- 6 Tab
local codeTab   = AddTab("Code", "💻", 1)
local moveTab   = AddTab("Di Chuyển", "🏃", 2)
local playerTab = AddTab("Người Chơi", "👤", 3)
local envTab    = AddTab("Môi Trường", "⚙️", 4)
local camTab    = AddTab("Camera", "📷", 5)
local gfxTab    = AddTab("Đồ Họa", "🎨", 6)

tabs[1].BackgroundColor3=C.BLUE
tabs[1].BackgroundTransparency=0.2
tabContent[1].Visible=true
activeTab=tabContent[1]

-- ==================== STATE ====================
local S = {
    flySpeed=50, carpetSize=2, walkSpeed=16, jumpPower=50, -- carpetSize đã thu nhỏ về 2 cực nhỏ
    noClip=false, fog=false, flying=false, carpet=false, flyRun=false,
    customCam=false, fov=70, camOffX=0, camOffY=0,
    freeCam=false, freeCamSpeed=30, carpetY=nil,
    shadows=true, bloom=true, blur=false, sunRays=true, colorCorr=false,
    fpsCounter=false, particles=true, decals=true,
    infJump=false, clickTp=false, esp=false, antiAfk=true, fullBright=false,
}

local scripts = {}
local totalRuns, cancelled = 0, false
local curThread, curIndicator = nil, nil
local char = player.Character or player.CharacterAdded:Wait()

-- ==================== HELPERS ====================
local function Hum() return char and char:FindFirstChildOfClass("Humanoid") end
local function Root() return char and char:FindFirstChild("HumanoidRootPart") end

local function UpdChar()
    local h = Hum()
    if h then
        h.WalkSpeed=S.walkSpeed
        h.JumpPower=S.jumpPower
    end
end

-- ==================== ANTI AFK ====================
player.Idled:Connect(function()
    if S.antiAfk then
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
    end
end)

-- ==================== INFINITE JUMP / FLY RUN JUMP ====================
UserInputService.JumpRequest:Connect(function()
    if S.infJump and char and Hum() then
        Hum():ChangeState(Enum.HumanoidStateType.Jumping)
    end
    -- Hỗ trợ nhảy khi dùng Thảm Kính / Bay Chạy Bộ
    if (S.carpet or S.flyRun) and char and Root() then
        S.carpetY = Root().Position.Y + 2.5
    end
end)

-- ==================== CLICK TELEPORT ====================
UserInputService.InputBegan:Connect(function(i, gp)
    if gp then return end
    if S.clickTp and i.UserInputType == Enum.UserInputType.MouseButton1 and UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
        local mouse = player:GetMouse()
        local r = Root()
        if r and mouse.Hit then
            r.CFrame = CFrame.new(mouse.Hit.Position + Vector3.new(0, 3, 0))
        end
    end
end)

-- ==================== NOCLIP ====================
local function EnNC()
    RunService:BindToRenderStep("NC", 1, function()
        if not S.noClip or not char then return end
        for _, p in ipairs(char:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide=false end
        end
    end)
end

local function DisNC()
    RunService:UnbindFromRenderStep("NC")
    if char then
        for _, p in ipairs(char:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide=true end
        end
    end
end

-- ==================== FLY SYSTEM ====================
local mobile = New("Frame", {
    Size=UDim2.new(0,180,0,70), Position=UDim2.new(0,10,1,-80),
    BackgroundTransparency=1, Visible=false, ZIndex=10,
}, gui)

local function MobBtn(t, p)
    local b = New("TextButton", {
        Size=UDim2.new(0,38,0,38), Position=p, Text=t,
        BackgroundColor3=C.WHITE, BackgroundTransparency=0.3,
        TextColor3=C.DARK, Font=Enum.Font.GothamBold, TextSize=18, BorderSizePixel=0, ZIndex=11,
    }, mobile)
    Corner(b, UDim.new(1,0))
    Stroke(b, nil, 1.5)
    return b
end

local upBtn = MobBtn("⏫", UDim2.new(0,8,0,8))
local dnBtn = MobBtn("⏬", UDim2.new(0,134,0,8))
local mInput = {up=false, down=false}
local tMap = {}

UserInputService.TouchStarted:Connect(function(i, p)
    if p then return end
    local pos = i.Position
    for _, d in ipairs({{upBtn,"up"},{dnBtn,"down"}}) do
        local b, k = d[1], d[2]
        local ap = b.AbsolutePosition
        local as = b.AbsoluteSize
        if pos.X>=ap.X and pos.X<=ap.X+as.X and pos.Y>=ap.Y and pos.Y<=ap.Y+as.Y then
            mInput[k]=true; tMap[i]=k; break
        end
    end
end)

UserInputService.TouchEnded:Connect(function(i)
    if tMap[i] then mInput[tMap[i]]=false; tMap[i]=nil end
end)

local function VInput()
    return UserInputService:IsKeyDown(Enum.KeyCode.Space) or mInput.up,
           UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) or mInput.down
end

local bv, bg, fp, cp, bp
local StopFlyRun, TogFlyRun

local function StopFly()
    if bv then bv:Destroy(); bv=nil end
    if bg then bg:Destroy(); bg=nil end
    if fp then fp:Destroy(); fp=nil end
    local h = Hum()
    if h then h.AutoRotate=true; h.PlatformStand=false end
    RunService:UnbindFromRenderStep("Fly")
    mobile.Visible=false
end

local function StopCarpet()
    if cp then cp:Destroy(); cp=nil end
    if bp then bp:Destroy(); bp=nil end
    RunService:UnbindFromRenderStep("Carpet")
    S.carpetY=nil
end

-- Cập nhật Thảm Kính: Nhỏ gọn siêu vi dưới chân + Trong suốt gần như không thấy
local function CreateCarpet(y)
    local r = Root()
    if not r then return end
    cp = New("Part", {
        Name="Carpet", Size=Vector3.new(S.carpetSize, 0.1, S.carpetSize), -- Siêu mỏng & nhỏ
        Transparency=0.95, -- Gần như tàng hình hoàn toàn không thấy dưới chân
        Color=Color3.fromRGB(200,230,255), Material=Enum.Material.Glass,
        Anchored=true, CanCollide=true, Position=Vector3.new(r.Position.X, y, r.Position.Z),
    }, workspace)
    
    S.carpetY = y

    RunService:BindToRenderStep("Carpet", 1, function()
        if not S.carpet or not cp or not r then return end
        local x, z = r.Position.X, r.Position.Z
        
        -- Thảm giữ nguyên độ cao tuyệt đối, không tự bay lên ngoại trừ bạn nâng Y hoặc Nhảy
        cp.Position = Vector3.new(x, S.carpetY or (r.Position.Y - 3.2), z)
    end)
end

local function TogFly()
    local r=Root()
    local h=Hum()
    if not r then return end

    if S.flying then
        if S.carpet then StopCarpet(); S.carpet=false end
        if S.flyRun and StopFlyRun then StopFlyRun() end
        bv=New("BodyVelocity",{MaxForce=Vector3.new(4000,4000,4000)},r)
        bg=New("BodyGyro",{MaxTorque=Vector3.new(4000,4000,4000)},r)
        if h then h.AutoRotate=false; h.PlatformStand=true end
        if not fp then
            fp=New("Part",{
                Name="Floor", Size=Vector3.new(6,0.2,6), Transparency=0.7,
                Color=Color3.fromRGB(200,230,255), Material=Enum.Material.Glass,
                Anchored=true, CanCollide=false,
            }, workspace)
        end
        RunService:BindToRenderStep("Fly",1,function()
            if not S.flying or not r or not bv then return end
            local d=h and h.MoveDirection or Vector3.zero
            local t=r.CFrame:VectorToWorldSpace(d)
            if t.Magnitude>0 then t=t.Unit end
            local u,dn=VInput()
            local vv=(u and 1 or 0)-(dn and 1 or 0)
            bv.Velocity=(t+Vector3.new(0,vv,0))*S.flySpeed
            bg.CFrame=CFrame.new(r.Position,r.Position+workspace.CurrentCamera.CFrame.LookVector)
            fp.Position=r.Position-Vector3.new(0,3.5,0)
        end)
    else
        StopFly()
    end
    mobile.Visible=S.flying and UserInputService.TouchEnabled
end

local function TogCarpet()
    local r=Root()
    if not r then return end
    if S.carpet then
        if S.flying then S.flying=false; StopFly() end
        CreateCarpet(r.Position.Y-3.2)
    else
        StopCarpet()
    end
end

-- Fly Run Overlay
local oLay = New("Frame", {
    Size=UDim2.new(0,180,0,160), Position=UDim2.new(1,-190,0.5,-80),
    BackgroundTransparency=1, Visible=false, ZIndex=10,
}, gui)

local function OBtn(t,y,s,c)
    local b = New("TextButton", {
        Size=UDim2.new(0,s,0,s), Position=UDim2.new(0.5,-s/2,0,y), Text=t,
        BackgroundColor3=c or C.BLUE, BackgroundTransparency=0.3, TextColor3=C.WHITE,
        Font=Enum.Font.GothamBold, TextSize=20, BorderSizePixel=0, ZIndex=11,
    }, oLay)
    Corner(b, UDim.new(1,0))
    Stroke(b, C.WHITE, 2)
    return b
end

local togCBtn = OBtn("🪩",0,50,C.GRAY)
local movUBtn = OBtn("⬆",60,50,Color3.fromRGB(0,150,0))
local movDBtn = OBtn("⬇",120,50,Color3.fromRGB(150,0,0))

local clsOBtn = New("TextButton", {
    Size=UDim2.new(0,34,0,34), Position=UDim2.new(1,-44,0,10), Text="✕",
    BackgroundColor3=C.RED, BackgroundTransparency=0.3, TextColor3=C.WHITE,
    Font=Enum.Font.GothamBold, TextSize=18, BorderSizePixel=0, ZIndex=11,
}, oLay)
Corner(clsOBtn,UDim.new(1,0))
Stroke(clsOBtn,C.WHITE,2)

local function StartFlyRun()
    local r=Root()
    if not r then return end
    if S.flying then S.flying=false; StopFly() end
    if not S.carpet then
        S.carpet=true; CreateCarpet(r.Position.Y-3.2)
    else
        S.carpetY=r.Position.Y-3.2
    end
    main.Visible=false; togBtn.Text="⚙"; oLay.Visible=true
end

StopFlyRun = function()
    if S.carpet then StopCarpet(); S.carpet=false end
    oLay.Visible=false; S.flyRun=false
end

TogFlyRun = function()
    if S.flyRun then StartFlyRun() else StopFlyRun() end
end

clsOBtn.MouseButton1Click:Connect(function() S.flyRun=false; StopFlyRun() end)
togCBtn.MouseButton1Click:Connect(function() if S.flyRun then S.carpet=not S.carpet; TogCarpet() end end)
movUBtn.MouseButton1Click:Connect(function()
    if S.flyRun then if not S.carpet then S.carpet=true; TogCarpet() end; S.carpetY=(S.carpetY or 0)+3 end
end)
movDBtn.MouseButton1Click:Connect(function()
    if S.flyRun then if not S.carpet then S.carpet=true; TogCarpet() end; S.carpetY=(S.carpetY or 0)-3 end
end)

-- ==================== CUSTOM CAMERA ====================
local ccX, ccY = 0, 10
local rMouse = false

local function DisCC()
    RunService:UnbindFromRenderStep("CC")
    local cam = workspace.CurrentCamera
    if cam then cam.CameraType=Enum.CameraType.Custom; cam.FieldOfView=70 end
end

local function EnCC()
    local cam = workspace.CurrentCamera
    if not cam then return end
    ccX=0; ccY=10
    cam.CameraType=Enum.CameraType.Scriptable
    cam.FieldOfView=S.fov
    RunService:BindToRenderStep("CC", Enum.RenderPriority.Camera.Value, function()
        if not S.customCam then return end
        local r=Root()
        if not r then return end
        local h=char and char:FindFirstChild("Head")
        local bp2=h and h.Position or r.Position
        local cf=CFrame.new(bp2) * CFrame.Angles(0,math.rad(ccX),0) * CFrame.Angles(math.rad(ccY),0,0)
        local off=cf.RightVector * S.camOffX + cf.UpVector * S.camOffY
        local cp2=bp2 + cf.LookVector*(-8) + off + Vector3.new(0,1.5,0)
        cam.CFrame=CFrame.new(cp2, bp2 + Vector3.new(0,1.5,0))
        cam.FieldOfView=S.fov
    end)
end

local function TogCC()
    if S.freeCam then return end
    if S.customCam then EnCC() else DisCC() end
end

UserInputService.InputChanged:Connect(function(i)
    if S.customCam and rMouse and i.UserInputType==Enum.UserInputType.MouseMovement then
        ccX=(ccX - i.Delta.X * 0.35)%360
        ccY=math.clamp(ccY - i.Delta.Y * 0.35,-75,75)
    end
end)

-- ==================== FREE CAMERA ====================
local fcPos = Vector3.new(0,10,0)
local fcYaw, fcPitch = 0, 0

local fcHUD = New("Frame", {
    Size=UDim2.new(0,220,0,36), Position=UDim2.new(0.5,-110,0,10),
    BackgroundColor3=Color3.new(0,0,0), BackgroundTransparency=0.5,
    Visible=false, BorderSizePixel=0, ZIndex=20,
}, gui)
Corner(fcHUD,UDim.new(0,8))
Stroke(fcHUD,Color3.fromRGB(0,200,255),1.5)

New("TextLabel", {
    Size=UDim2.new(1,-10,1,0), Position=UDim2.new(0,8,0,0),
    Text="🎥 Camera Tự Do | [F] Thoát | Shift tăng tốc",
    BackgroundTransparency=1, TextColor3=Color3.fromRGB(0,220,255),
    Font=Enum.Font.GothamBold, TextSize=10, TextXAlignment=Enum.TextXAlignment.Left, ZIndex=21,
}, fcHUD)

local cross = New("Frame", {
    Size=UDim2.new(0,6,0,6), Position=UDim2.new(0.5,-3,0.5,-3),
    BackgroundColor3=C.WHITE, BackgroundTransparency=0.3, BorderSizePixel=0, Visible=false, ZIndex=20,
}, gui)
Corner(cross,UDim.new(1,0))

local function DisFC()
    RunService:UnbindFromRenderStep("FC")
    local cam=workspace.CurrentCamera
    if cam then cam.CameraType=Enum.CameraType.Custom; cam.FieldOfView=70 end
    UserInputService.MouseBehavior=Enum.MouseBehavior.Default
    UserInputService.MouseIconEnabled=true
    local r=Root()
    if r then r.Anchored=false end
    local h=Hum()
    if h then h.AutoRotate=true; h.WalkSpeed=S.walkSpeed; h.JumpPower=S.jumpPower end
    fcHUD.Visible=false; cross.Visible=false
end

local function EnFC()
    local cam=workspace.CurrentCamera
    if not cam then return end
    local r=Root()
    local h=Hum()
    if r then fcPos=r.Position+Vector3.new(0,5,0); r.Anchored=true end
    if h then h.AutoRotate=false; h.WalkSpeed=0; h.JumpPower=0 end
    cam.CameraType=Enum.CameraType.Scriptable
    cam.FieldOfView=90
    fcYaw=0; fcPitch=0
    UserInputService.MouseBehavior=Enum.MouseBehavior.LockCenter
    UserInputService.MouseIconEnabled=false
    fcHUD.Visible=true; cross.Visible=true
    RunService:BindToRenderStep("FC", Enum.RenderPriority.Camera.Value, function(dt)
        if not S.freeCam then return end
        local d=UserInputService:GetMouseDelta()
        fcYaw=(fcYaw - d.X * 0.3)%360
        fcPitch=math.clamp(fcPitch - d.Y * 0.3,-89,89)
        local cf=CFrame.new(fcPos) * CFrame.Angles(0,math.rad(fcYaw),0) * CFrame.Angles(math.rad(fcPitch),0,0)
        local sp=S.freeCamSpeed * dt
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then sp=sp * 3 end
        local m=Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then m+=cf.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then m-=cf.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then m-=cf.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then m+=cf.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.E) then m+=Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.Q) then m-=Vector3.new(0,1,0) end
        fcPos+=m*sp
        cam.CFrame=CFrame.new(fcPos) * CFrame.Angles(0,math.rad(fcYaw),0) * CFrame.Angles(math.rad(fcPitch),0,0)
    end)
end

local function TogFC()
    if S.customCam then S.customCam=false; DisCC() end
    if S.freeCam then EnFC() else DisFC() end
end

UserInputService.InputBegan:Connect(function(i,gp)
    if gp then return end
    if i.KeyCode==Enum.KeyCode.F and S.freeCam then S.freeCam=false; TogFC() end
    if i.UserInputType==Enum.UserInputType.MouseButton2 then
        rMouse=true
        if S.customCam then UserInputService.MouseBehavior=Enum.MouseBehavior.LockCurrentPosition end
    end
end)

UserInputService.InputEnded:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.MouseButton2 then
        rMouse=false
        if S.customCam then UserInputService.MouseBehavior=Enum.MouseBehavior.Default end
    end
end)

-- ==================== ESP SYSTEM ====================
local espFolder = Instance.new("Folder")
espFolder.Name = "ExMenu_ESP"
espFolder.Parent = gui

local activeESPs = {}

local function RemovePlayerESP(p)
    if activeESPs[p] then
        if activeESPs[p].HL then activeESPs[p].HL:Destroy() end
        if activeESPs[p].BB then activeESPs[p].BB:Destroy() end
        if activeESPs[p].Thread then task.cancel(activeESPs[p].Thread) end
        activeESPs[p] = nil
    end
end

local function ClearAllESP()
    for p, _ in pairs(activeESPs) do
        RemovePlayerESP(p)
    end
    espFolder:ClearAllChildren()
end

local function CreatePlayerESP(p)
    RemovePlayerESP(p)

    if not S.esp or p == player then return end

    local pChar = p.Character
    if not pChar then return end

    local rPart = pChar:WaitForChild("HumanoidRootPart", 5)
    local hum = pChar:WaitForChild("Humanoid", 5)

    if not rPart or not hum or hum.Health <= 0 then return end

    local hl = Instance.new("Highlight")
    hl.Name = p.Name .. "_HL"
    hl.Adornee = pChar
    hl.FillColor = Color3.fromRGB(255, 50, 50)
    hl.FillTransparency = 0.5
    hl.OutlineColor = Color3.fromRGB(255, 255, 255)
    hl.OutlineTransparency = 0
    hl.Parent = espFolder

    local bb = Instance.new("BillboardGui")
    bb.Name = p.Name .. "_BB"
    bb.Adornee = rPart
    bb.Size = UDim2.new(0, 150, 0, 40)
    bb.StudsOffset = Vector3.new(0, 3.5, 0)
    bb.AlwaysOnTop = true
    bb.Parent = espFolder

    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 11
    lbl.TextStrokeTransparency = 0.2
    lbl.Parent = bb

    local thread = task.spawn(function()
        while S.esp and pChar and pChar.Parent and hum and hum.Health > 0 do
            local r = Root()
            if r then
                local dist = math.round((r.Position - rPart.Position).Magnitude)
                lbl.Text = string.format("%s\n❤️ %d/%d | 📏 %dm", p.Name, math.round(hum.Health), math.round(hum.MaxHealth), dist)
            else
                lbl.Text = string.format("%s\n❤️ %d/%d | 📏 --m", p.Name, math.round(hum.Health), math.round(hum.MaxHealth))
            end
            task.wait(0.15)
        end
        RemovePlayerESP(p)
    end)

    activeESPs[p] = {HL = hl, BB = bb, Thread = thread}
end

local function HookPlayer(p)
    if p == player then return end

    p.CharacterAdded:Connect(function()
        if S.esp then
            task.wait(0.5)
            CreatePlayerESP(p)
        end
    end)

    p.CharacterRemoving:Connect(function()
        RemovePlayerESP(p)
    end)

    if p.Character and S.esp then
        CreatePlayerESP(p)
    end
end

local function UpdateESP()
    ClearAllESP()
    if not S.esp then return end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            CreatePlayerESP(p)
        end
    end
end

for _, p in ipairs(Players:GetPlayers()) do HookPlayer(p) end
Players.PlayerAdded:Connect(function(p) HookPlayer(p) end)
Players.PlayerRemoving:Connect(function(p) RemovePlayerESP(p) end)

-- ==================== CODE EXECUTION ====================
local function ExecOnce(code, name)
    if #name>0 then print("👤 Chạy bởi:", name) end
    return pcall(function()
        local fn, err = loadstring(code)
        if not fn then error(err) end
        fn()
    end)
end

local function Cancel()
    cancelled=true
    if curThread then task.cancel(curThread); curThread=nil end
    if curIndicator then curIndicator.BackgroundColor3=C.BLUE; curIndicator=nil end
end

local function RunCode(code, name, ind, times, delay)
    Cancel()
    if #code==0 then return false, "⚠️ Vui lòng nhập code!" end
    cancelled=false
    if ind then curIndicator=ind; ind.BackgroundColor3=C.RED end
    local okC, failC = 0, 0
    curThread=task.spawn(function()
        for i=1,times do
            if cancelled then break end
            if i>1 and delay>0 then
                local e=0
                while e<delay do
                    if cancelled then break end
                    task.wait(0.1); e+=0.1
                end
                if cancelled then break end
            end
            local ok, err = ExecOnce(code, name)
            if ok then okC+=1 else failC+=1; warn("❌ Lần",i,err) end
        end
        totalRuns+=okC+failC
        if ind then ind.BackgroundColor3=C.BLUE; if curIndicator==ind then curIndicator=nil end end
        curThread=nil
    end)
    return true, nil
end

-- ==================== BUILD UI HELPERS ====================
local function Label(parent, text, y)
    return New("TextLabel", {
        Size=UDim2.new(1,-16,0,14), Position=UDim2.new(0,8,0,y or 0),
        Text=text, BackgroundTransparency=1, TextColor3=Color3.fromRGB(60,60,60),
        Font=Enum.Font.GothamMedium, TextSize=10, TextXAlignment=Enum.TextXAlignment.Left, ZIndex=6,
    }, parent)
end

local function Button(parent, text, x, y, w, h, color)
    local btn = New("TextButton", {
        Size=UDim2.new(0,w or 100,0,h or 24), Position=UDim2.new(0,x or 8,0,y or 0),
        Text=text, BackgroundColor3=color or C.GRAY, BackgroundTransparency=0.2,
        TextColor3=C.WHITE, Font=Enum.Font.GothamBold, TextSize=10, BorderSizePixel=0, ZIndex=6,
    }, parent)
    Corner(btn, UDim.new(0,5))
    Stroke(btn, color and color:Lerp(Color3.new(0,0,0),0.4) or nil, 1)
    btn.MouseEnter:Connect(function() Tween(btn, {BackgroundTransparency=0.05}, 0.2) end)
    btn.MouseLeave:Connect(function() Tween(btn, {BackgroundTransparency=0.2}, 0.2) end)
    return btn
end

-- ==================== TAB 1: CODE ====================
local y = 8

Label(codeTab, "⚡ Code Nhanh - Nhấn để chạy ngay", y)
y = y + 16

local quickScripts = {
    {n="Dex Explorer", d="Mở Dex Explorer", c=[[loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))()]], cl=Color3.fromRGB(50,120,200)},
    {n="Infinite Yield", d="Admin Commands", c=[[loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()]], cl=C.PURPLE},
    {n="SimpleSpy v3", d="Theo dõi RemoteEvent & RemoteFunction", c=[[loadstring(game:HttpGet("https://raw.githubusercontent.com/ex-serum/SimpleSpy/main/SimpleSpy.lua"))()]], cl=Color3.fromRGB(0,150,80)},
}

for _, s in ipairs(quickScripts) do
    local btn = New("TextButton", {
        Size=UDim2.new(1,-16,0,28), Position=UDim2.new(0,8,0,y), Text="",
        BackgroundColor3=s.cl, BackgroundTransparency=0.3, BorderSizePixel=0, ZIndex=6,
    }, codeTab)
    Corner(btn, UDim.new(0,5))
    Stroke(btn, s.cl, 1.2)
    New("TextLabel", {
        Size=UDim2.new(1,-10,1,0), Position=UDim2.new(0,10,0,0), Text=s.n.."\n"..s.d,
        BackgroundTransparency=1, TextColor3=C.WHITE, Font=Enum.Font.GothamBold, TextSize=10,
        TextXAlignment=Enum.TextXAlignment.Left, TextYAlignment=Enum.TextYAlignment.Center, ZIndex=7,
    }, btn)
    btn.MouseButton1Click:Connect(function() RunCode(s.c, s.n, nil, 1, 0) end)
    y = y + 32
end

y = y + 6
Label(codeTab, "━━━━━━━━━━━━━━━━━━━━━━", y)
y = y + 16

Label(codeTab, "💻 Nhập Code Tùy Chỉnh", y)
y = y + 14
Label(codeTab, "👤 Tên Script", y)
y = y + 14

local nameIn = New("TextBox", {
    Size=UDim2.new(1,-16,0,26), Position=UDim2.new(0,8,0,y), Text="",
    PlaceholderText="Nhập tên script...", PlaceholderColor3=Color3.fromRGB(160,160,160),
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false,
    Active=true, Selectable=true, ZIndex=10, TextXAlignment=Enum.TextXAlignment.Left,
}, codeTab)
Corner(nameIn, UDim.new(0,5))
Stroke(nameIn, Color3.fromRGB(100,120,200), 1.5)
New("UIPadding", {PaddingLeft=UDim.new(0,6)}, nameIn)

y = y + 32
Label(codeTab, "💻 Code (Lua)", y)
y = y + 14

local codeIn = New("TextBox", {
    Size=UDim2.new(1,-16,0,80), Position=UDim2.new(0,8,0,y), Text="",
    PlaceholderText="-- Nhập code Lua tại đây...", PlaceholderColor3=Color3.fromRGB(160,160,160),
    BackgroundColor3=Color3.fromRGB(245,245,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.Code, TextSize=11, BorderSizePixel=0, ClearTextOnFocus=false,
    MultiLine=true, TextWrapped=true, TextXAlignment=Enum.TextXAlignment.Left, TextYAlignment=Enum.TextYAlignment.Top,
    Active=true, Selectable=true, ZIndex=10,
}, codeTab)
Corner(codeIn, UDim.new(0,5))
Stroke(codeIn, Color3.fromRGB(100,120,200), 1.5)
New("UIPadding", {PaddingLeft=UDim.new(0,6), PaddingTop=UDim.new(0,4)}, codeIn)

y = y + 86

-- Loop settings
Label(codeTab, "🔁 Cài đặt lặp", y)
y = y + 14
Label(codeTab, "Số lần lặp:", y)

local repIn = New("TextBox", {
    Size=UDim2.new(0,55,0,24), Position=UDim2.new(0,8,0,y+12), Text="1",
    PlaceholderColor3=Color3.fromRGB(160,160,160), BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0,
    TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0,
    ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=10,
}, codeTab)
Corner(repIn, UDim.new(0,5))
Stroke(repIn, Color3.fromRGB(180,180,200), 1.2)

Label(codeTab, "Thời gian chờ:", y+36)

local delIn = New("TextBox", {
    Size=UDim2.new(0,55,0,24), Position=UDim2.new(0,8,0,y+50), Text="0",
    PlaceholderColor3=Color3.fromRGB(160,160,160), BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0,
    TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0,
    ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=10,
}, codeTab)
Corner(delIn, UDim.new(0,5))
Stroke(delIn, Color3.fromRGB(180,180,200), 1.2)

local unitBtn = New("TextButton", {
    Size=UDim2.new(0,55,0,24), Position=UDim2.new(0,75,0,y+50), Text="Giây ▾",
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=C.DARK,
    Font=Enum.Font.GothamBold, TextSize=9, BorderSizePixel=0, ZIndex=10,
}, codeTab)
Corner(unitBtn,UDim.new(0,4)); Stroke(unitBtn)

local ddFrame = New("Frame", {
    Size=UDim2.new(0,55,0,48), Position=UDim2.new(0,75,0,y+74),
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, BorderSizePixel=0, Visible=false, ZIndex=15,
}, codeTab)
Corner(ddFrame,UDim.new(0,4)); Stroke(ddFrame)

local secOpt = New("TextButton", {
    Size=UDim2.new(1,0,0,24), Text="Giây", BackgroundColor3=Color3.fromRGB(245,245,245),
    BackgroundTransparency=0, TextColor3=C.DARK, Font=Enum.Font.GothamBold, TextSize=9, BorderSizePixel=0, ZIndex=16,
}, ddFrame)

local minOpt = New("TextButton", {
    Size=UDim2.new(1,0,0,24), Position=UDim2.new(0,0,0,24), Text="Phút",
    BackgroundColor3=Color3.fromRGB(245,245,245), BackgroundTransparency=0, TextColor3=C.DARK,
    Font=Enum.Font.GothamBold, TextSize=9, BorderSizePixel=0, ZIndex=16,
}, ddFrame)

unitBtn.MouseButton1Click:Connect(function() ddFrame.Visible=not ddFrame.Visible end)
secOpt.MouseButton1Click:Connect(function() unitBtn.Text="Giây ▾"; ddFrame.Visible=false end)
minOpt.MouseButton1Click:Connect(function() unitBtn.Text="Phút ▾"; ddFrame.Visible=false end)

UserInputService.InputBegan:Connect(function(i,gp)
    if gp then return end
    if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
        local objs=gui:GetGuiObjectsAtPosition(i.Position.X,i.Position.Y)
        local f=false
        for _,o in ipairs(objs) do
            if o==unitBtn or o:IsDescendantOf(ddFrame) then f=true; break end
        end
        if not f then ddFrame.Visible=false end
    end
end)

y = y + 82

local runBtn = Button(codeTab, "▶ Chạy Code", 8, y, 140, 26, Color3.fromRGB(0,160,90))
local stopBtn = Button(codeTab, "⏹ Dừng", 156, y, 80, 26, C.RED)
y = y + 32
local saveBtn = Button(codeTab, "💾 Lưu Script", 8, y, 140, 26, C.BLUE)
y = y + 32

local statusLbl = Label(codeTab, "", y)
statusLbl.TextColor3=Color3.fromRGB(220,170,0); statusLbl.TextSize=9; statusLbl.ZIndex=6
y = y + 14

local countLbl = Label(codeTab, "🔄 Tổng số lần đã chạy: 0", y)
countLbl.TextColor3=C.GREEN; countLbl.TextSize=9; countLbl.ZIndex=6

local RebuildScripts

stopBtn.MouseButton1Click:Connect(function() Cancel(); statusLbl.Text="⏹️ Đã dừng" end)

runBtn.MouseButton1Click:Connect(function()
    local t=math.clamp(tonumber(repIn.Text)or 1,1,1000)
    local d=math.max(tonumber(delIn.Text)or 0,0)
    if unitBtn.Text:find("Phút") then d=d*60 end
    local ok,err=RunCode(codeIn.Text,nameIn.Text,nil,t,d)
    if not ok then
        statusLbl.Text=err or "❌ Lỗi không xác định"
    else
        statusLbl.Text="⏳ Đang thực thi..."
        task.spawn(function()
            while curThread do
                if cancelled then statusLbl.Text="⏹️ Đã dừng"; return end
                task.wait(0.1)
            end
            if not cancelled then statusLbl.Text="✅ Hoàn thành!" end
            countLbl.Text="🔄 Tổng số lần đã chạy: "..totalRuns
        end)
    end
end)

saveBtn.MouseButton1Click:Connect(function()
    local n=nameIn.Text
    local c=codeIn.Text
    if #c==0 then statusLbl.Text="⚠️ Vui lòng nhập code!"; return end
    if #n==0 then n="Script "..(#scripts+1) end
    local bn=n
    local cnt=1
    while true do
        local ex=false
        for _,s in ipairs(scripts) do if s.name==n then ex=true; break end end
        if not ex then break end
        cnt+=1; n=bn.." ("..cnt..")"
    end
    table.insert(scripts,{name=n,code=c})
    RebuildScripts()
    statusLbl.Text="✅ Đã lưu: "..n
end)

y = y + 20
Label(codeTab, "━━━━━━━━━━━━━━━━━━━━━━", y)
y = y + 16
Label(codeTab, "💾 Script Đã Lưu (▶ chạy | ✏ sửa | ✕ xóa)", y)
y = y + 16

local searchIn = New("TextBox", {
    Size=UDim2.new(1,-16,0,26), Position=UDim2.new(0,8,0,y), Text="",
    PlaceholderText="🔍 Tìm kiếm script theo tên...", PlaceholderColor3=Color3.fromRGB(160,160,160),
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false,
    Active=true, Selectable=true, ZIndex=10, TextXAlignment=Enum.TextXAlignment.Left,
}, codeTab)
Corner(searchIn, UDim.new(0,5))
Stroke(searchIn, Color3.fromRGB(180,180,200), 1.2)
New("UIPadding", {PaddingLeft=UDim.new(0,6)}, searchIn)
y = y + 32

local scriptList = New("Frame", {
    Size=UDim2.new(1,-16,0,0), Position=UDim2.new(0,8,0,y),
    BackgroundTransparency=1, BorderSizePixel=0, ZIndex=6,
}, codeTab)
New("UIListLayout", {SortOrder=Enum.SortOrder.LayoutOrder, Padding=UDim.new(0,3)}, scriptList)

RebuildScripts = function()
    for _,c in ipairs(scriptList:GetChildren()) do
        if not c:IsA("UIListLayout") then c:Destroy() end
    end

    local term=searchIn.Text:lower()
    local disp={}
    for _,d in ipairs(scripts) do
        if term=="" or d.name:lower():find(term,1,true) then table.insert(disp,d) end
    end

    if #disp==0 then
        New("TextLabel", {
            Size=UDim2.new(1,0,0,30),
            Text=term~="" and "📭 Không tìm thấy script nào" or "📭 Chưa có script nào được lưu",
            BackgroundTransparency=1, TextColor3=C.GRAY, Font=Enum.Font.GothamMedium, TextSize=10,
            TextXAlignment=Enum.TextXAlignment.Center, TextYAlignment=Enum.TextYAlignment.Center, ZIndex=7,
        }, scriptList)
    end

    for i,d in ipairs(disp) do
        local row = New("Frame", {
            Size=UDim2.new(1,0,0,46), BackgroundColor3=Color3.fromRGB(255,255,255),
            BackgroundTransparency=0.1, BorderSizePixel=0, ZIndex=6,
        }, scriptList)
        Corner(row,UDim.new(0,5)); Stroke(row)

        local infoFrame = New("Frame", {
            Size=UDim2.new(1,-56,0,38), Position=UDim2.new(0,6,0,4), BackgroundTransparency=1, ZIndex=7,
        }, row)

        local rn = New("TextButton", {
            Size=UDim2.new(1,0,1,0), Text="", BackgroundTransparency=1, BorderSizePixel=0, ZIndex=7,
        }, infoFrame)

        New("TextLabel", {
            Size=UDim2.new(1,0,0,16), Text=d.name, BackgroundTransparency=1, TextColor3=C.DARK,
            Font=Enum.Font.GothamBold, TextSize=10, TextXAlignment=Enum.TextXAlignment.Left, ZIndex=8,
        }, infoFrame)

        New("TextLabel", {
            Size=UDim2.new(1,0,0,14), Position=UDim2.new(0,0,0,18),
            Text=d.code:gsub("%s+"," "):sub(1,45)..(#d.code>45 and "..." or ""),
            BackgroundTransparency=1, TextColor3=Color3.fromRGB(120,120,120),
            Font=Enum.Font.GothamMedium, TextSize=8, TextXAlignment=Enum.TextXAlignment.Left, ZIndex=8,
        }, infoFrame)

        local ind = New("TextButton", {
            Size=UDim2.new(0,20,0,20), Position=UDim2.new(1,-50,0,4), Text="▶",
            BackgroundColor3=C.GREEN, BackgroundTransparency=0.1, TextColor3=C.WHITE,
            Font=Enum.Font.GothamBold, TextSize=9, BorderSizePixel=0, ZIndex=8,
        }, row)
        Corner(ind,UDim.new(1,0))

        local edit = New("TextButton", {
            Size=UDim2.new(0,20,0,20), Position=UDim2.new(1,-50,0,26), Text="✏",
            BackgroundColor3=C.BLUE, BackgroundTransparency=0.1, TextColor3=C.WHITE,
            Font=Enum.Font.GothamBold, TextSize=9, BorderSizePixel=0, ZIndex=8,
        }, row)
        Corner(edit,UDim.new(1,0))

        local del = New("TextButton", {
            Size=UDim2.new(0,20,0,20), Position=UDim2.new(1,-26,0,4), Text="✕",
            BackgroundColor3=C.RED, BackgroundTransparency=0.1, TextColor3=C.WHITE,
            Font=Enum.Font.GothamBold, TextSize=9, BorderSizePixel=0, ZIndex=8,
        }, row)
        Corner(del,UDim.new(1,0))

        local idx=i
        ind.MouseButton1Click:Connect(function()
            if curIndicator==ind then Cancel() return end
            RunCode(d.code,d.name,ind,1,0)
            statusLbl.Text="⏳ Đang chạy: "..d.name
        end)
        edit.MouseButton1Click:Connect(function()
            nameIn.Text=d.name
            codeIn.Text=d.code
            codeTab.CanvasPosition=Vector2.new(0,0)
        end)
        del.MouseButton1Click:Connect(function()
            if curIndicator==ind then Cancel() end
            table.remove(scripts,idx)
            RebuildScripts()
        end)

        rn.MouseEnter:Connect(function() Tween(row,{BackgroundTransparency=0},0.15) end)
        rn.MouseLeave:Connect(function() Tween(row,{BackgroundTransparency=0.1},0.15) end)
    end

    local listH = #disp>0 and #disp*49 or 33
    scriptList.Size=UDim2.new(1,-16,0,listH)
    codeTab.CanvasSize=UDim2.new(0,0,0,y+listH+20)
end

searchIn:GetPropertyChangedSignal("Text"):Connect(RebuildScripts)
RebuildScripts()

-- ==================== TAB 2: DI CHUYỂN ====================
local my = 8

Label(moveTab, "⚡ Tốc độ bay", my)
my = my + 14

local flySpdIn = New("TextBox", {
    Size=UDim2.new(0,55,0,24), Position=UDim2.new(0,8,0,my), Text="50",
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false,
    Active=true, Selectable=true, ZIndex=8,
}, moveTab)
Corner(flySpdIn,UDim.new(0,5)); Stroke(flySpdIn)

local flyBtn = Button(moveTab, "🕊️ Bay: TẮT", 72, my, 130, 24, C.GRAY)
my = my + 30

Label(moveTab, "🪩 Thảm kính", my)
my = my + 14
local carpBtn = Button(moveTab, "🪩 Thảm kính: TẮT", 8, my, 130, 24, C.GRAY)
local flyRunBtn = Button(moveTab, "🕹️ Bay chạy bộ: TẮT", 148, my, 130, 24, C.GRAY)
my = my + 30

Label(moveTab, "📏 Kích thước thảm (studs)", my)
my = my + 14

local carpSzIn = New("TextBox", {
    Size=UDim2.new(0,55,0,24), Position=UDim2.new(0,8,0,my), Text="2", -- Mặc định là 2 siêu nhỏ
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false,
    Active=true, Selectable=true, ZIndex=8,
}, moveTab)
Corner(carpSzIn,UDim.new(0,5)); Stroke(carpSzIn)

my = my + 30

Label(moveTab, "🚪 Bổ trợ di chuyển đặc biệt", my)
my = my + 14
local noclipBtn = Button(moveTab, "🚶 NoClip: TẮT", 8, my, 130, 24, C.GRAY)
local infJumpBtn = Button(moveTab, "🦘 Nhảy Vô Hạn: TẮT", 148, my, 130, 24, C.GRAY)
my = my + 30

local clickTpBtn = Button(moveTab, "🎯 Click TP (Ctrl+Click): TẮT", 8, my, 210, 24, C.GRAY)
my = my + 32

Label(moveTab, "━━━━━━━━━━━━━━━━━━━━━━", my)
my = my + 16

Label(moveTab, "🏃 Tốc độ chạy (WalkSpeed)", my)
my = my + 14

local wsIn = New("TextBox", {
    Size=UDim2.new(0,55,0,24), Position=UDim2.new(0,8,0,my), Text="16",
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false,
    Active=true, Selectable=true, ZIndex=8,
}, moveTab)
Corner(wsIn,UDim.new(0,5)); Stroke(wsIn)

local wsLbl = Label(moveTab, "Hiện tại: 16", my)
wsLbl.Position=UDim2.new(0,72,0,my); wsLbl.Size=UDim2.new(0,120,0,14)
wsLbl.TextColor3=C.GREEN; wsLbl.TextSize=9

my = my + 30

Label(moveTab, "📦 Lực nhảy (JumpPower)", my)
my = my + 14

local jpIn = New("TextBox", {
    Size=UDim2.new(0,55,0,24), Position=UDim2.new(0,8,0,my), Text="50",
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false,
    Active=true, Selectable=true, ZIndex=8,
}, moveTab)
Corner(jpIn,UDim.new(0,5)); Stroke(jpIn)

local jpLbl = Label(moveTab, "Hiện tại: 50", my)
jpLbl.Position=UDim2.new(0,72,0,my); jpLbl.Size=UDim2.new(0,120,0,14)
jpLbl.TextColor3=C.GREEN; jpLbl.TextSize=9

-- Connect controls
flySpdIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(flySpdIn.Text); if n and n>=0 then S.flySpeed=n else flySpdIn.Text=tostring(S.flySpeed) end end)
carpSzIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(carpSzIn.Text); if n and n>=0.5 then S.carpetSize=n else carpSzIn.Text=tostring(S.carpetSize) end end)
wsIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(wsIn.Text); if n and n>0 then S.walkSpeed=n; UpdChar() else wsIn.Text=tostring(S.walkSpeed) end; wsLbl.Text="Hiện tại: "..S.walkSpeed end)
jpIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(jpIn.Text); if n and n>0 then S.jumpPower=n; UpdChar() else jpIn.Text=tostring(S.jumpPower) end; jpLbl.Text="Hiện tại: "..S.jumpPower end)

flyBtn.MouseButton1Click:Connect(function()
    S.flying = not S.flying
    TogFly()
    flyBtn.Text=S.flying and "🕊️ Bay: BẬT" or "🕊️ Bay: TẮT"
    flyBtn.BackgroundColor3=S.flying and Color3.fromRGB(70,130,180) or C.GRAY
end)
carpBtn.MouseButton1Click:Connect(function()
    S.carpet = not S.carpet
    TogCarpet()
    carpBtn.Text=S.carpet and "🪩 Thảm kính: BẬT" or "🪩 Thảm kính: TẮT"
    carpBtn.BackgroundColor3=S.carpet and Color3.fromRGB(160,100,200) or C.GRAY
end)
flyRunBtn.MouseButton1Click:Connect(function()
    S.flyRun = not S.flyRun
    TogFlyRun()
    flyRunBtn.Text=S.flyRun and "🕹️ Bay chạy bộ: BẬT" or "🕹️ Bay chạy bộ: TẮT"
    flyRunBtn.BackgroundColor3=S.flyRun and C.ORANGE or C.GRAY
end)
noclipBtn.MouseButton1Click:Connect(function()
    S.noClip=not S.noClip
    noclipBtn.Text=S.noClip and "🚶 NoClip: BẬT" or "🚶 NoClip: TẮT"
    noclipBtn.BackgroundColor3=S.noClip and Color3.fromRGB(0,180,100) or C.GRAY
    if S.noClip then EnNC() else DisNC() end
end)
infJumpBtn.MouseButton1Click:Connect(function()
    S.infJump=not S.infJump
    infJumpBtn.Text=S.infJump and "🦘 Nhảy Vô Hạn: BẬT" or "🦘 Nhảy Vô Hạn: TẮT"
    infJumpBtn.BackgroundColor3=S.infJump and C.GREEN or C.GRAY
end)
clickTpBtn.MouseButton1Click:Connect(function()
    S.clickTp=not S.clickTp
    clickTpBtn.Text=S.clickTp and "🎯 Click TP (Ctrl+Click): BẬT" or "🎯 Click TP (Ctrl+Click): TẮT"
    clickTpBtn.BackgroundColor3=S.clickTp and C.GREEN or C.GRAY
end)

moveTab.CanvasSize=UDim2.new(0,0,0,my+40)

-- ==================== TAB 3: NGƯỜI CHƠI ====================
local py = 8

Label(playerTab, "👁️ Quan sát & Nhìn xuyên tường", py)
py = py + 14

local espBtn = Button(playerTab, "👁️ ESP Người Chơi: TẮT", 8, py, 200, 24, C.GRAY)
espBtn.MouseButton1Click:Connect(function()
    S.esp = not S.esp
    espBtn.Text = S.esp and "👁️ ESP Người Chơi: BẬT" or "👁️ ESP Người Chơi: TẮT"
    espBtn.BackgroundColor3 = S.esp and C.GREEN or C.GRAY
    UpdateESP()
end)
py = py + 30

Label(playerTab, "🛡️ Treo máy & Hệ thống", py)
py = py + 14

local antiAfkBtn = Button(playerTab, "🛡️ Anti-AFK (Treo máy): BẬT", 8, py, 200, 24, C.GREEN)
antiAfkBtn.MouseButton1Click:Connect(function()
    S.antiAfk = not S.antiAfk
    antiAfkBtn.Text = S.antiAfk and "🛡️ Anti-AFK (Treo máy): BẬT" or "🛡️ Anti-AFK (Treo máy): TẮT"
    antiAfkBtn.BackgroundColor3 = S.antiAfk and C.GREEN or C.GRAY
end)
py = py + 32

Label(playerTab, "━━━━━━━━━━━━━━━━━━━━━━", py)
py = py + 16

Label(playerTab, "🔄 Chuyển máy chủ", py)
py = py + 14

local rejoinBtn = Button(playerTab, "🔄 Vào Lại Server", 8, py, 110, 24, C.BLUE)
local serverHopBtn = Button(playerTab, "🌐 Đổi Server Khác", 126, py, 110, 24, C.PURPLE)

rejoinBtn.MouseButton1Click:Connect(function()
    TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, player)
end)

serverHopBtn.MouseButton1Click:Connect(function()
    local servers = {}
    local req = request or http_request or (syn and syn.request)
    if req then
        local res = req({Url = string.format("https://games.roblox.com/v1/games/%d/servers/0?sortOrder=Desc&limit=100", game.PlaceId)})
        local body = HttpService:JSONDecode(res.Body)
        if body and body.data then
            for _, s in ipairs(body.data) do
                if type(s) == "table" and s.playing < s.maxPlayers and s.id ~= game.JobId then
                    table.insert(servers, s.id)
                end
            end
        end
    end
    if #servers > 0 then
        TeleportService:TeleportToPlaceInstance(game.PlaceId, servers[math.random(1, #servers)], player)
    else
        TeleportService:Teleport(game.PlaceId, player)
    end
end)

playerTab.CanvasSize = UDim2.new(0, 0, 0, py + 50)

-- ==================== TAB 4: MÔI TRƯỜNG ====================
local ey = 8

Label(envTab, "🌤 Điều khiển thời tiết", ey)
ey = ey + 14
local darkBtn = Button(envTab, "🌙 Trời tối", 8, ey, 130, 24, Color3.fromRGB(60,60,100))
local brightBtn = Button(envTab, "☀️ Trời sáng", 148, ey, 130, 24, C.YELLOW)
ey = ey + 30

local fogBtn = Button(envTab, "🌫️ Sương mù: TẮT", 8, ey, 130, 24, C.GRAY)
local fullBrightBtn = Button(envTab, "💡 Sáng Đêm: TẮT", 148, ey, 130, 24, C.GRAY)
ey = ey + 30

local resetEnvBtn = Button(envTab, "🔄 Đặt lại mặc định", 8, ey, 130, 24, C.GRAY)
ey = ey + 36

Label(envTab, "💡 Độ sáng (0.5 - 3.0)", ey)
ey = ey + 14

local brightIn = New("TextBox", {
    Size=UDim2.new(0,60,0,24), Position=UDim2.new(0,8,0,ey), Text="1.2",
    BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20),
    Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false,
    Active=true, Selectable=true, ZIndex=8,
}, envTab)
Corner(brightIn,UDim.new(0,5)); Stroke(brightIn)

local applyBrightBtn = Button(envTab, "✔ Áp dụng", 78, ey, 80, 24, C.BLUE)
ey = ey + 30

Label(envTab, "🎨 Màu môi trường (R G B)", ey)
ey = ey + 14

local aR = New("TextBox", {Size=UDim2.new(0,40,0,24), Position=UDim2.new(0,8,0,ey), Text="128", BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=8}, envTab)
Corner(aR,UDim.new(0,5)); Stroke(aR)
local aG = New("TextBox", {Size=UDim2.new(0,40,0,24), Position=UDim2.new(0,54,0,ey), Text="128", BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=8}, envTab)
Corner(aG,UDim.new(0,5)); Stroke(aG)
local aB = New("TextBox", {Size=UDim2.new(0,40,0,24), Position=UDim2.new(0,100,0,ey), Text="128", BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=8}, envTab)
Corner(aB,UDim.new(0,5)); Stroke(aB)
local applyAmbBtn = Button(envTab, "✔ Áp dụng", 150, ey, 80, 24, C.BLUE)

darkBtn.MouseButton1Click:Connect(function()
    TweenL({ClockTime=0, Brightness=0.3, OutdoorAmbient=Color3.fromRGB(20,20,40), Ambient=Color3.fromRGB(20,20,40)})
end)
brightBtn.MouseButton1Click:Connect(function()
    TweenL({ClockTime=14, Brightness=2.5, OutdoorAmbient=C.WHITE, Ambient=C.WHITE, EnvironmentDiffuseScale=1, EnvironmentSpecularScale=1})
    if S.fog then S.fog=false; fogBtn.Text="🌫️ Sương mù: TẮT"; fogBtn.BackgroundColor3=C.GRAY; Tween(Lighting,{FogEnd=100000,FogStart=0,FogColor=Color3.new(1,1,1)}) end
end)
fogBtn.MouseButton1Click:Connect(function()
    S.fog=not S.fog
    fogBtn.Text=S.fog and "🌫️ Sương mù: BẬT" or "🌫️ Sương mù: TẮT"
    fogBtn.BackgroundColor3=S.fog and Color3.fromRGB(70,70,70) or C.GRAY
    TweenL(S.fog and {FogStart=0,FogEnd=80,FogColor=Color3.fromRGB(180,180,180)} or {FogStart=0,FogEnd=100000,FogColor=Color3.new(1,1,1)})
end)

fullBrightBtn.MouseButton1Click:Connect(function()
    S.fullBright = not S.fullBright
    fullBrightBtn.Text = S.fullBright and "💡 Sáng Đêm: BẬT" or "💡 Sáng Đêm: TẮT"
    fullBrightBtn.BackgroundColor3 = S.fullBright and C.GREEN or C.GRAY
    if S.fullBright then
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
        Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
        Lighting.Brightness = 2
    else
        TweenL(DEFAULT_LIGHTING)
    end
end)

resetEnvBtn.MouseButton1Click:Connect(function()
    TweenL(DEFAULT_LIGHTING)
    S.fog=false; fogBtn.Text="🌫️ Sương mù: TẮT"; fogBtn.BackgroundColor3=C.GRAY
    S.fullBright=false; fullBrightBtn.Text="💡 Sáng Đêm: TẮT"; fullBrightBtn.BackgroundColor3=C.GRAY
end)
applyBrightBtn.MouseButton1Click:Connect(function()
    local n=tonumber(brightIn.Text)
    if n and n>=0.5 and n<=3 then TweenL({Brightness=n}) else brightIn.Text=string.format("%.1f",Lighting.Brightness) end
end)
applyAmbBtn.MouseButton1Click:Connect(function()
    local r=math.clamp(tonumber(aR.Text)or 128,0,255)
    local g=math.clamp(tonumber(aG.Text)or 128,0,255)
    local b=math.clamp(tonumber(aB.Text)or 128,0,255)
    TweenL({Ambient=Color3.fromRGB(r,g,b)})
end)

envTab.CanvasSize=UDim2.new(0,0,0,ey+50)

-- ==================== TAB 5: CAMERA ====================
local cy = 8

Label(camTab, "📷 Camera Tùy Chỉnh (Bám nhân vật)", cy)
cy = cy + 14
local ccBtn = Button(camTab, "📷 Camera tùy chỉnh: TẮT", 8, cy, 200, 24, C.GRAY)
cy = cy + 30

Label(camTab, "Góc nhìn FOV (30-120)", cy)
cy = cy + 14

local fovIn = New("TextBox", {Size=UDim2.new(0,60,0,24), Position=UDim2.new(0,8,0,cy), Text="70", BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=8}, camTab)
Corner(fovIn,UDim.new(0,5)); Stroke(fovIn)
cy = cy + 30

Label(camTab, "Độ lệch ngang (Offset X)", cy)
cy = cy + 14

local offXIn = New("TextBox", {Size=UDim2.new(0,60,0,24), Position=UDim2.new(0,8,0,cy), Text="0", BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=8}, camTab)
Corner(offXIn,UDim.new(0,5)); Stroke(offXIn)
cy = cy + 30

Label(camTab, "Độ lệch dọc (Offset Y)", cy)
cy = cy + 14

local offYIn = New("TextBox", {Size=UDim2.new(0,60,0,24), Position=UDim2.new(0,8,0,cy), Text="0", BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=8}, camTab)
Corner(offYIn,UDim.new(0,5)); Stroke(offYIn)
cy = cy + 34

Label(camTab, "━━━━━━━━━━━━━━━━━━━━━━", cy)
cy = cy + 16

Label(camTab, "🎥 Camera Tự Do (Xuyên tường)", cy)
cy = cy + 14
local fcBtn = Button(camTab, "🎥 Camera tự do: TẮT", 8, cy, 200, 24, C.GRAY)
cy = cy + 30

Label(camTab, "⚡ Tốc độ di chuyển camera", cy)
cy = cy + 14

local fcSpdIn = New("TextBox", {Size=UDim2.new(0,60,0,24), Position=UDim2.new(0,8,0,cy), Text="30", BackgroundColor3=Color3.fromRGB(255,255,255), BackgroundTransparency=0, TextColor3=Color3.fromRGB(20,20,20), Font=Enum.Font.GothamMedium, TextSize=12, BorderSizePixel=0, ClearTextOnFocus=false, Active=true, Selectable=true, ZIndex=8}, camTab)
Corner(fcSpdIn,UDim.new(0,5)); Stroke(fcSpdIn)
cy = cy + 32

local helpFrame = New("Frame", {
    Size=UDim2.new(1,-16,0,60), Position=UDim2.new(0,8,0,cy), BackgroundColor3=Color3.fromRGB(230,245,255),
    BackgroundTransparency=0.1, BorderSizePixel=0, ZIndex=6,
}, camTab)
Corner(helpFrame,UDim.new(0,6)); Stroke(helpFrame,Color3.fromRGB(100,160,220),1.2)
New("TextLabel", {
    Size=UDim2.new(1,-16,1,-10), Position=UDim2.new(0,8,0,5),
    Text="📌 WASD: Di chuyển | Q/E: Lên/Xuống\n🖱 Chuột: Xoay camera | Shift: Tăng tốc\nF: Thoát camera tự do | Nhân vật đứng yên",
    BackgroundTransparency=1, TextColor3=Color3.fromRGB(40,80,120), Font=Enum.Font.GothamMedium,
    TextSize=10, TextXAlignment=Enum.TextXAlignment.Left, TextYAlignment=Enum.TextYAlignment.Top, BorderSizePixel=0, ZIndex=7,
}, helpFrame)

ccBtn.MouseButton1Click:Connect(function()
    S.customCam = not S.customCam
    TogCC()
    ccBtn.Text=S.customCam and "📷 Camera tùy chỉnh: BẬT" or "📷 Camera tùy chỉnh: TẮT"
    ccBtn.BackgroundColor3=S.customCam and C.ORANGE or C.GRAY
end)
fcBtn.MouseButton1Click:Connect(function()
    S.freeCam = not S.freeCam
    TogFC()
    fcBtn.Text=S.freeCam and "🎥 Camera tự do: BẬT" or "🎥 Camera tự do: TẮT"
    fcBtn.BackgroundColor3=S.freeCam and Color3.fromRGB(0,160,200) or C.GRAY
end)
fovIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(fovIn.Text); if n and n>=30 and n<=120 then S.fov=n else fovIn.Text=tostring(S.fov) end end)
offXIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(offXIn.Text); if n then S.camOffX=n else offXIn.Text=tostring(S.camOffX) end end)
offYIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(offYIn.Text); if n then S.camOffY=n else offYIn.Text=tostring(S.camOffY) end end)
fcSpdIn.FocusLost:Connect(function(e) if not e then return end; local n=tonumber(fcSpdIn.Text); if n and n>0 then S.freeCamSpeed=n else fcSpdIn.Text=tostring(S.freeCamSpeed) end end)

camTab.CanvasSize=UDim2.new(0,0,0,cy+80)

-- ==================== TAB 6: ĐỒ HỌA ====================
local gy = 8

Label(gfxTab, "📊 Chất lượng đồ họa", gy)
gy = gy + 14
local qualLbl = Label(gfxTab, "📊 Chất lượng: Tự động", gy)
qualLbl.TextColor3=C.GREEN; qualLbl.TextSize=9
gy = gy + 16

local presetBtns = {
    {n="✨ Siêu cao", c=Color3.fromRGB(180,50,50), p="Ultra"},
    {n="🔵 Cao", c=Color3.fromRGB(50,100,180), p="High"},
    {n="🟡 Trung", c=Color3.fromRGB(180,140,30), p="Medium"},
    {n="🟢 Thấp", c=Color3.fromRGB(60,160,80), p="Low"},
}
for i,d in ipairs(presetBtns) do
    local btn = Button(gfxTab, d.n, 8+(i-1)*72, gy, 64, 22, d.c)
    btn.MouseButton1Click:Connect(function()
        local pr=PRESETS[d.p]
        if not pr then return end
        pcall(function() settings().Rendering.QualityLevel=pr.level end)
        Lighting.ShadowSoftness=pr.shadow; Lighting.GlobalShadows=pr.shadows
        Lighting.EnvironmentDiffuseScale=pr.diffuse; Lighting.EnvironmentSpecularScale=pr.specular
        qualLbl.Text="📊 Chất lượng: "..pr.name
        qualLbl.TextColor3=d.p=="Ultra" and Color3.fromRGB(220,80,80) or C.GREEN
        S.shadows=pr.shadows
    end)
end
gy = gy + 28

local ulBtn = Button(gfxTab, "⚡ Siêu thấp - Tối đa FPS", 8, gy, 220, 22, Color3.fromRGB(100,60,60))
ulBtn.MouseButton1Click:Connect(function()
    local pr=PRESETS.UltraLow
    pcall(function() settings().Rendering.QualityLevel=pr.level end)
    Lighting.ShadowSoftness=pr.shadow; Lighting.GlobalShadows=pr.shadows
    Lighting.EnvironmentDiffuseScale=pr.diffuse; Lighting.EnvironmentSpecularScale=pr.specular
    qualLbl.Text="📊 Chất lượng: "..pr.name
    qualLbl.TextColor3=C.GREEN
    S.shadows=pr.shadows
end)

gy = gy + 30
Label(gfxTab, "🌑 Bóng đổ", gy)
gy = gy + 14
local shadowBtn = Button(gfxTab, "🌑 Bóng đổ: BẬT", 8, gy, 160, 22, C.GREEN)
shadowBtn.MouseButton1Click:Connect(function()
    S.shadows=not S.shadows
    Lighting.GlobalShadows=S.shadows
    shadowBtn.Text=S.shadows and "🌑 Bóng đổ: BẬT" or "🌑 Bóng đổ: TẮT"
    shadowBtn.BackgroundColor3=S.shadows and C.GREEN or C.GRAY
end)

gy = gy + 30
Label(gfxTab, "✨ Hiệu ứng hậu kỳ", gy)
gy = gy + 14

local be=Lighting:FindFirstChild("HQ_Bloom") or New("BloomEffect",{Name="HQ_Bloom",Enabled=true,Intensity=0.5},Lighting)
local ble=Lighting:FindFirstChild("HQ_Blur") or New("BlurEffect",{Name="HQ_Blur",Enabled=false,Size=12},Lighting)
local sre=Lighting:FindFirstChild("HQ_SunRays") or New("SunRaysEffect",{Name="HQ_SunRays",Enabled=true,Intensity=0.25},Lighting)
local cce=Lighting:FindFirstChild("HQ_ColorFix") or New("ColorCorrectionEffect",{Name="HQ_ColorFix",Enabled=false,Contrast=0.15,Saturation=0.2},Lighting)

local effects = {
    {t="🌸 Bloom: BẬT", s="bloom", e=be, on=C.GREEN, off=C.GRAY},
    {t="🌫 Làm mờ: TẮT", s="blur", e=ble, on=C.GREEN, off=C.GRAY},
    {t="☀ Tia nắng: BẬT", s="sunRays", e=sre, on=C.GREEN, off=C.GRAY},
    {t="🎨 Màu sắc: TẮT", s="colorCorr", e=cce, on=C.ORANGE, off=C.GRAY},
}

for i,d in ipairs(effects) do
    local x = i%2==1 and 8 or 148
    local yy = gy + (i-1)//2 * 28
    local isOn = not d.t:find("TẮT")
    local btn = Button(gfxTab, d.t, x, yy, 130, 22, isOn and d.on or d.off)
    btn.MouseButton1Click:Connect(function()
        S[d.s]=not S[d.s]
        d.e.Enabled=S[d.s]
        if S[d.s] then
            btn.Text=btn.Text:gsub("TẮT","BẬT")
            btn.BackgroundColor3=d.on
        else
            btn.Text=btn.Text:gsub("BẬT","TẮT")
            btn.BackgroundColor3=d.off
        end
    end)
end

gy = gy + 60
Label(gfxTab, "📈 Đếm FPS", gy)
gy = gy + 14
local fpsBtn = Button(gfxTab, "📈 Hiện FPS: TẮT", 8, gy, 160, 22, C.GRAY)

local fpsLbl = New("TextLabel", {
    Size=UDim2.new(0,90,0,22), Position=UDim2.new(0,10,1,-30), Text="",
    BackgroundColor3=Color3.new(0,0,0), BackgroundTransparency=0.5,
    TextColor3=Color3.fromRGB(0,255,100), Font=Enum.Font.GothamBold, TextSize=13,
    Visible=false, BorderSizePixel=0, ZIndex=10,
}, gui)
Corner(fpsLbl,UDim.new(0,6))

local fpsBuf={}
fpsBtn.MouseButton1Click:Connect(function()
    S.fpsCounter=not S.fpsCounter
    fpsLbl.Visible=S.fpsCounter
    fpsBtn.Text=S.fpsCounter and "📈 Hiện FPS: BẬT" or "📈 Hiện FPS: TẮT"
    fpsBtn.BackgroundColor3=S.fpsCounter and Color3.fromRGB(0,180,80) or C.GRAY
    if S.fpsCounter then
        RunService:BindToRenderStep("FPS", Enum.RenderPriority.Last.Value, function(dt)
            table.insert(fpsBuf,dt)
            if #fpsBuf>30 then table.remove(fpsBuf,1) end
            if #fpsBuf>0 then
                local t=0
                for _,v in ipairs(fpsBuf) do t+=v end
                if t>0 then
                    local fps=math.round(#fpsBuf/t)
                    fpsLbl.TextColor3=fps>=55 and Color3.fromRGB(0,255,100) or fps>=30 and Color3.fromRGB(255,200,0) or Color3.fromRGB(255,60,60)
                    fpsLbl.Text="FPS: "..fps
                end
            end
        end)
    else
        RunService:UnbindFromRenderStep("FPS")
        fpsBuf={}
        fpsLbl.Text=""
    end
end)

gy = gy + 30
Label(gfxTab, "🖼 Chất lượng hình ảnh", gy)
gy = gy + 14
local texHighBtn = Button(gfxTab, "🔵 Cao", 8, gy, 82, 22, Color3.fromRGB(50,100,180))
local texLowBtn = Button(gfxTab, "🟢 Thấp", 96, gy, 82, 22, Color3.fromRGB(60,160,80))
local texOffBtn = Button(gfxTab, "⚫ Tắt", 184, gy, 82, 22, C.GRAY)

local function setTextures(mode)
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Texture") or obj:IsA("Decal") then
            obj.Transparency = mode=="Off" and 1 or 0
        end
    end
end
texHighBtn.MouseButton1Click:Connect(function() setTextures("High") end)
texLowBtn.MouseButton1Click:Connect(function() setTextures("Low") end)
texOffBtn.MouseButton1Click:Connect(function() setTextures("Off") end)

gy = gy + 30
Label(gfxTab, "💨 Hiệu ứng hạt & Hình dán", gy)
gy = gy + 14
local partBtn = Button(gfxTab, "💨 Hạt: BẬT", 8, gy, 130, 22, C.GREEN)
local decalBtn = Button(gfxTab, "🖼 Hình dán: BẬT", 148, gy, 130, 22, C.GREEN)

local function setParticles(enabled)
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then
            obj.Enabled=enabled
        end
    end
end
local function setDecals(enabled)
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Decal") then
            obj.Transparency=enabled and 0 or 1
        end
    end
end

partBtn.MouseButton1Click:Connect(function()
    S.particles=not S.particles
    setParticles(S.particles)
    partBtn.Text=S.particles and "💨 Hạt: BẬT" or "💨 Hạt: TẮT"
    partBtn.BackgroundColor3=S.particles and C.GREEN or C.GRAY
end)
decalBtn.MouseButton1Click:Connect(function()
    S.decals=not S.decals
    setDecals(S.decals)
    decalBtn.Text=S.decals and "🖼 Hình dán: BẬT" or "🖼 Hình dán: TẮT"
    decalBtn.BackgroundColor3=S.decals and C.GREEN or C.GRAY
end)

gy = gy + 32
local resetGfxBtn = Button(gfxTab, "🔄 Đặt lại đồ họa về mặc định", 8, gy, 240, 24, Color3.fromRGB(150,60,60))
resetGfxBtn.MouseButton1Click:Connect(function()
    local pr=PRESETS.Ultra
    pcall(function() settings().Rendering.QualityLevel=pr.level end)
    Lighting.ShadowSoftness=pr.shadow; Lighting.GlobalShadows=pr.shadows
    Lighting.EnvironmentDiffuseScale=pr.diffuse; Lighting.EnvironmentSpecularScale=pr.specular
    TweenL(DEFAULT_LIGHTING)
    be.Enabled=true; ble.Enabled=false; sre.Enabled=true; cce.Enabled=false
    S.bloom=true; S.blur=false; S.sunRays=true; S.colorCorr=false
    S.shadows=true; S.particles=true; S.decals=true
    setParticles(true); setDecals(true); setTextures("High")
    brightIn.Text="1.2"
    qualLbl.Text="📊 Chất lượng: ✨ Siêu cao"
    qualLbl.TextColor3=Color3.fromRGB(220,80,80)
end)

gfxTab.CanvasSize=UDim2.new(0,0,0,gy+40)

-- ==================== TOGGLE MENU ====================
closeBtn.MouseButton1Click:Connect(function()
    main.Visible=false
    togBtn.Text="🍌"
end)

togBtn.MouseButton1Click:Connect(function()
    if S.flyRun then return end
    local v=not main.Visible
    main.Visible=v
    togBtn.Text=v and "✕" or "🍌"
end)

-- ==================== CHARACTER RESPAWN & AUTO RESTORE STATE ====================
player.CharacterAdded:Connect(function(c)
    char = c
    task.wait(0.2)

    UpdChar()
    if S.noClip then EnNC() end
    if S.flying then StopFly(); TogFly() end
    if S.carpet then StopCarpet(); TogCarpet() end
    if S.flyRun then TogFlyRun() end
    if S.customCam then EnCC() end
    if S.freeCam then EnFC() end
    if S.esp then UpdateESP() end
end)

-- ==================== INITIALIZATION ====================
UpdChar()
if S.noClip then EnNC() end

print("✅ Banana Cat Executor Hub (Đã Tùy Chỉnh Thảm Kính Tàng Hình & Bay Chạy Bộ) sẵn sàng!")
